import { jest, expect, test } from '@jest/globals';
import * as storageService from '../services/storage-service';

test('storage get -- success', async () => {

    // storageService.storage = {
    //     get: jest.fn(() => 'got')
    // }

    // expect(storageService.get('key')).toBe('got');

});